{
  "name": "buttplug-mcp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "bun run scripts/install-engine.ts",
    "start": "bun run index.ts",
    "build": "bun build --target=bun index.ts --outfile=dist/server.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0"
  }
}
